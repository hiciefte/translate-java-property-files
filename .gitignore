venv/
__pycache__/
*.pyc
*.iml
.idea/
# User-specific files
.DS_Store

# Logs
/logs/
!/logs/.gitkeep

# Secrets
# This directory contains sensitive information like API keys and should never be committed.
/secrets/
!/secrets/.gitkeep

# Environment files. The .env file in the docker directory is for server deployment secrets.
# It should be ignored to prevent accidental commits of sensitive data.
/.env
docker/.env
docker/docker-compose.override.yml
# Ignore local config file, but track the example.
/config.yaml
