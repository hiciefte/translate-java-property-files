# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info

# IDE settings
.idea/
*.iml
.vscode/

# User-specific files
.DS_Store

# Logs
# Ignore contents of logs, but not the directory itself, so .gitkeep can be tracked.
/logs/*
!/logs/.gitkeep

# Secrets
# This directory contains sensitive information like API keys and should never be committed.
# Ignore contents of secrets, but not the directory itself, so .gitkeep can be tracked.
/secrets/*
!/secrets/.gitkeep

# Environment files.
# Catches .env, .env.local, .env.*, etc.
/.env*
docker/.env*

# Docker Compose override files
docker/docker-compose.override.yml
docker/docker-compose.override.yaml
docker/docker-compose.override.*.yml
docker/docker-compose.override.*.yaml
