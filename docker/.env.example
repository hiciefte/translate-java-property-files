# This is an example .env file.
# On a server deployment, copy this file to '.env' within the 'docker' directory
# and fill in all the required values.

# --- Instance-Specific Identifiers (CRITICAL for multi-deployment) ---
# A unique name for this service's Docker container.
# Example: bisq_desktop_translator
CONTAINER_NAME=translation_service_runner
# A unique name for this service's Docker volume to prevent data collision.
# Example: bisq_desktop_repo_data
VOLUME_NAME=target-repo-data

# --- API Keys and Tokens (SECRETS) ---
OPENAI_API_KEY=your_openai_api_key
TX_TOKEN=your_transifex_api_token
GITHUB_TOKEN=your_github_personal_access_token_with_repo_scope

# --- Health Check URL (Optional) ---
# Used to ping a monitoring service at the end of a successful run.
HEALTHCHECK_URL=

# --- Git User Configuration (REQUIRED for Commits) ---
# This identity will be used to author the commits.
GIT_AUTHOR_NAME="Your Git Name"
GIT_AUTHOR_EMAIL="your-verified@email.com"

# The GPG key ID to use for signing commits.
# The public key must be uploaded to the GitHub account associated with GITHUB_TOKEN.
GIT_SIGNING_KEY=YOUR_GPG_KEY_ID

# --- Git Repository Configuration (Project Specific) ---
# The name of YOUR fork of the repository (e.g., 'your-github-username/bisq-desktop').
FORK_REPO_NAME="hiciefte/bisq2"

# The name of the main, upstream repository.
UPSTREAM_REPO_NAME="bisq-network/bisq2"

# The branch in the upstream repository that pull requests should target.
TARGET_BRANCH_FOR_PR="main"

# --- Docker Host User Mapping ---
# These values are used to map file permissions between the host and the container.
# Run 'id -u' and 'id -g' on your host machine to get these values.
HOST_UID=1000
HOST_GID=1000

# The GPG key fingerprint of the bot's key, used to establish trust within the container.
GPG_KEY_FINGERPRINT_FOR_TRUST=YOUR_BOT_GPG_KEY_FINGERPRINT